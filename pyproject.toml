[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "text-toolkit"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
features = ["extension-module"]
module-name = "text_toolkit"
